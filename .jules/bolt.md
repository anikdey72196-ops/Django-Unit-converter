## 2026-07-28 - Add Lazy Loading to Image Tags
**Learning:** Adding the `loading="lazy"` attribute natively defers loading of images below the fold, saving bandwidth and significantly boosting initial paint times with no JavaScript required and no adverse UI effects.
**Action:** Always prioritize adding `loading="lazy"` to images (especially large ones) that are out of the initial viewport in web applications to easily improve performance metrics.
