## 2024-08-01 - Missing ARIA Labels on Icon-Only Buttons
**Learning:** The application's core interactive buttons (mobile menu, swap, and copy) rely exclusively on SVGs without accessible text, representing a recurring accessibility issue pattern in the app's components.
**Action:** Ensure all icon-only buttons include explicit `aria-label` attributes for screen reader support.
